## 1. 基础结构搭建

- [x] 1.1 在 `miniprogram/app.json` 中注册新页面：`pages/lobby/index`、`pages/werewolf/index`
- [x] 1.2 创建 `pages/lobby/` 目录及四件套文件（index.js / index.json / index.wxml / index.wxss）
- [x] 1.3 创建 `pages/werewolf/` 目录及四件套文件（index.js / index.json / index.wxml / index.wxss）
- [x] 1.4 创建 `miniprogram/data/wordbank.js` 词库文件，内置 60+ 个游戏词语

## 2. 首页改造

- [x] 2.1 在 `pages/index/index.wxml` 中增加"聚会游戏"入口卡片
- [x] 2.2 在 `pages/index/index.js` 中添加跳转到游戏大厅的事件处理函数

## 3. 游戏大厅页面（game-lobby）

- [x] 3.1 实现大厅页面 UI：展示"狼人真言"游戏卡片（名称、简介、封面）
- [x] 3.2 实现点击游戏卡片跳转到 `/pages/werewolf/index`

## 4. 游戏设置阶段（room-management）

- [x] 4.1 实现设置页 UI：人数选择器（4～10），显示对应角色配置预览
- [x] 4.2 实现人数不足时禁用"开始游戏"按钮并提示
- [x] 4.3 实现"开始游戏"逻辑：随机分配村长和各玩家身份，写入页面 `data`

## 5. 角色查看阶段（werewolf-game）

- [x] 5.1 实现选词阶段 UI：村长查看 3 个随机词，点击选中一个作为咒语
- [x] 5.2 实现从词库随机抽取 3 个不重复词语的逻辑
- [x] 5.3 实现角色私密查看阶段 UI：按顺序展示"请将手机传给【XXX】"提示
- [x] 5.4 实现"按住查看"遮罩交互（touchstart 显示咒语，touchend 遮盖）
- [x] 5.5 实现"我已查看"确认按钮，推进到下一个需要查看的角色
- [x] 5.6 所有角色查看完毕后自动切换到倒计时阶段

## 6. 倒计时提问阶段（werewolf-game）

- [x] 6.1 实现 120 秒倒计时 UI（大字显示剩余秒数，进度条）
- [x] 6.2 实现基于时间戳的倒计时逻辑，使用 `setInterval` 每秒更新
- [x] 6.3 监听 `onHide` / `onShow` 生命周期，切换后台时根据时间戳修正剩余时间
- [x] 6.4 倒计时归零时自动跳转到结果阶段，判定狼人胜利
- [x] 6.5 实现"有人猜出了咒语！"按钮，点击后停止计时并判定村民方胜利

## 7. 游戏结果阶段（werewolf-game）

- [x] 7.1 实现结果页 UI：展示胜负标题（村民方胜利 / 狼人胜利）
- [x] 7.2 结果页列出所有玩家编号、身份（含村长标识）和咒语
- [x] 7.3 实现"再来一局"按钮：清空所有游戏状态，返回设置阶段

## 8. 状态机与页面流转

- [x] 8.1 在 `pages/werewolf/index.js` 中定义 `currentStep` 状态机及所有合法状态（setup / choose-spell / view-spell / countdown / result）
- [x] 8.2 在 `index.wxml` 中使用 `wx:if` 按 `currentStep` 值切换不同阶段的 UI 块
- [x] 8.3 确保页面返回/关闭时清理 `setInterval` 计时器，防止内存泄漏（`onUnload`）

## 9. 音频旁白系统

- [x] 9.1 新增音频文件：`close_eyes.mp3`、`open_eyes.mp3`、`wolf_wake.mp3`、`wolf_sleep.mp3`、`prophet_wake.mp3`、`prophet_sleep.mp3`
- [x] 9.2 使用 `wx.createInnerAudioContext` 创建音频实例，按流程顺序串联播放
- [x] 9.3 游戏开始时播放"请所有人闭眼"语音，延迟 3 秒后进入角色查看流程
- [x] 9.4 狼人/预言家查看咒语时播放对应的唤醒（wake）和入睡（sleep）提示音
- [x] 9.5 所有音频播放完毕后自动推进状态机到倒计时阶段

## 10. 4 方向桌游查看模式

- [x] 10.1 查看咒语阶段去除"长按查看"遮罩和手动"看完了"按钮，改为全员同时查看
- [x] 10.2 使用 CSS `transform: rotate()` 将咒语文字分别旋转 0°、90°、180°、270° 显示，适配桌面四方向玩家
- [x] 10.3 新增 `spellCountdown` 8 秒自动倒计时，计时结束后自动切换至下一阶段
- [x] 10.4 狼人和预言家分别有独立的 8 秒查看窗口，期间全桌可见咒语

## 11. 投票环节

- [x] 11.1 村民猜出咒语后，进入"狼人找预言家"投票环节（30 秒）
- [x] 11.2 若狼人在 30 秒内指出预言家，狼人反败为胜
- [x] 11.3 狼人未能在时限内指出预言家，进入"村民找狼人"投票环节（30 秒）
- [x] 11.4 村民在 30 秒内投票指出狼人，村民最终获胜；超时则狼人胜利
- [x] 11.5 投票计时器最后 10 秒数字变大、背景渐变为红色的动效
- [x] 11.6 使用独立的 `voteType`（`find-prophet` / `find-wolf`）区分两个投票阶段
- [x] 11.7 使用 `finalWinner` 字段解耦最终胜者与"是否猜出咒语"逻辑，避免结果页文案错误

## 12. 自定义导航栏

- [x] 12.1 在 `index.json` 中设置 `navigationStyle: custom`，隐藏默认标题栏
- [x] 12.2 在 `onLoad` 中调用 `wx.getSystemInfoSync()` 和 `wx.getMenuButtonBoundingClientRect()` 动态计算状态栏高度、导航栏高度及右侧安全距离
- [x] 12.3 自定义导航栏左侧放置"规则"和"返回"按钮，右侧留出胶囊按钮安全区
- [x] 12.4 实现游戏规则弹窗（modal），支持滚动、`word-break` 换行，`z-index` 覆盖导航栏

## 13. 流程细节与 UX 优化

- [x] 13.1 游戏开始增加"宣布首领"中间页（`announce-chief`），展示村长编号后再进入角色查看
- [x] 13.2 狼人/预言家查看咒语前增加"确认"过渡页（`confirm-hand`），防止传递手机时身份泄露
- [x] 13.3 倒计时时长改为玩家自定义，支持 1～10 分钟选择
- [x] 13.4 将首页模板内容（"快速了解云开发"等）清理干净，大厅页作为小程序首页
- [x] 13.5 结果页"再来一局"改为"返回"按钮，回到设置页重新配置